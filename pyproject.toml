[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "equity-regime-research"
version = "0.2.0"
description = "Statistical Evaluation of Momentum, Mean Reversion, and Regime Dynamics in U.S. Equity Markets"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
    "statsmodels>=0.14",
    "matplotlib>=3.7",
    "pyyaml>=6.0",
    "pyarrow>=12.0",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
    "pandas-datareader>=0.10",
    "yfinance>=0.2",
    "requests>=2.28",
    "jinja2>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
]

[project.scripts]
equity-regime = "equity_regime.pipeline:main_cli"
equity-etl    = "pipeline.etl:main_cli"

[tool.setuptools.packages.find]
where = ["."]
include = ["pipeline*", "backtest*"]

[tool.setuptools.package-dir]
"" = "."

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
markers = ["slow: marks tests as slow (full pipeline, deselect with -m 'not slow')"]
filterwarnings = [
    "ignore::RuntimeWarning:statsmodels",
    "ignore::statsmodels.tools.sm_exceptions.InterpolationWarning",
    "ignore:The test statistic is outside:UserWarning",
    "ignore::FutureWarning:yfinance",
    "ignore::FutureWarning:pandas_datareader",
]
